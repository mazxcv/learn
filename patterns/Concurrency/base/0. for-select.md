# For-Select цикл 

Требуется выполнять действие бесконечно до сигнала отмены

```golang

for {
    select {
        case <- done:
            return
        default:
    }

    // code 
}

```

```golang

for {
    select {
        case <- done:
            return
        default:
        // code 
    }
}

```